% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eDNA_dmm_nmds.R
\name{eDNA_dmm_nmds}
\alias{eDNA_dmm_nmds}
\title{NMDS ordination of community composition colored by community assignment}
\usage{
eDNA_dmm_nmds(
  fit,
  k = 2,
  nmds_axes = c(1, 2),
  distance = "bray",
  use_edna_index = TRUE,
  trymax = 100,
  seed = 42,
  show_ellipse = TRUE,
  ellipse_type = "t",
  community_colors = NULL,
  size_range = c(1.5, 5),
  alpha = 0.85,
  base_size = 13,
  title = NULL,
  subtitle = NULL,
  legend_position = "right"
)
}
\arguments{
\item{fit}{An \code{edna_dmm_fit} object from \code{\link[=eDNA_dmm]{eDNA_dmm()}}.}

\item{k}{A positive integer: the number of NMDS dimensions. Default is \code{2}.
Increasing to \code{3} can reduce stress but requires inspecting multiple
axis pairs.}

\item{nmds_axes}{An integer vector of length 2: which NMDS axes to plot.
Default is \code{c(1, 2)} (NMDS1 vs NMDS2). Change to \code{c(1, 3)} or \code{c(2, 3)}
to view other axis combinations when \code{k >= 3}.}

\item{distance}{A string: the dissimilarity index passed to
\code{\link[vegan:vegdist]{vegan::vegdist()}}. Default is \code{"bray"} (Bray-Curtis). Other options
include \code{"jaccard"}, \code{"euclidean"}, \code{"kulczynski"}. See
\code{\link[vegan:vegdist]{vegan::vegdist()}} for all options.}

\item{use_edna_index}{Logical. If \code{TRUE} (the default), apply the eDNA
index transformation before computing distances. If \code{FALSE}, raw
relative abundances (row sums normalized to 1) are used.}

\item{trymax}{A positive integer: the maximum number of random starts for
NMDS. Default is \code{100}. More starts reduce the risk of local optima but
increase computation time.}

\item{seed}{A single integer: the random seed for NMDS. Default is \code{42}.
Set for reproducibility.}

\item{show_ellipse}{Logical. If \code{TRUE} (the default), draw a 95\% confidence
ellipse per community.}

\item{ellipse_type}{A string: the type of ellipse passed to
\code{\link[ggplot2:stat_ellipse]{ggplot2::stat_ellipse()}}. Default is \code{"t"} (t-distribution based, more
robust with few points). Use \code{"norm"} for a normal-distribution based
ellipse.}

\item{community_colors}{A named character vector mapping community names to
hex colors. Pass \code{NULL} (the default) for the automatic HCL palette.}

\item{size_range}{A numeric vector of length 2: the minimum and maximum
point sizes, representing assignment certainties of 50\% and 100\%
respectively. Default is \code{c(1.5, 5)}.}

\item{alpha}{A number in (0, 1]: the transparency of points. Default \code{0.85}.}

\item{base_size}{A positive number: the base font size for the plot theme.
Default is \code{13}.}

\item{title}{A string: the plot title. Default is auto-generated.}

\item{subtitle}{Plot subtitle. Default \code{NULL}, meaning no subtitle.}

\item{legend_position}{A string: legend placement. One of \code{"right"}
(default), \code{"bottom"}, \code{"left"}, \code{"top"}, or \code{"none"}.}
}
\value{
A list with two elements:
\describe{
\item{\code{plot}}{A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object showing the NMDS ordination.}
\item{\code{nmds}}{The \code{\link[vegan:metaMDS]{vegan::metaMDS()}} result object, for advanced use
(e.g., adding species scores with \code{vegan::scores()}).}
}
}
\description{
Runs a non-metric multidimensional scaling (NMDS) ordination on the sample
community composition and plots the result, with points colored by their
MAP community assignment from the DMM. Point size reflects assignment
certainty: larger points are more confidently assigned.

NMDS is performed using \code{\link[vegan:metaMDS]{vegan::metaMDS()}} on Bray-Curtis dissimilarities
computed from the eDNA index transformation. The eDNA index (Kelly et al.
2019) normalizes read counts to relative abundances within each sample and
then divides by the across-sample maximum for each taxon, which reduces
the influence of dominant taxa on distance calculations.

The function returns a \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object that can be further
customized.
}
\section{Axes displayed}{

\code{nmds_axes} controls which two NMDS axes to display. For \code{k = 2}
dimensions (the default), only axes 1 and 2 exist. For \code{k = 3}, you can
pass \code{nmds_axes = c(1, 3)} to view NMDS1 vs NMDS3. Use multiple calls to
inspect all axis pairs when fitting in 3D.
}

\section{NMDS stress}{

The stress value (a measure of how well 2D positions represent the true
dissimilarities) is shown in the subtitle. Rough guidelines:
\itemize{
\item Stress < 0.05: excellent representation
\item Stress 0.05-0.10: good
\item Stress 0.10-0.20: adequate, but interpret with care
\item Stress > 0.20: poor; consider \code{k = 3} and examining multiple axis pairs
}
}

\section{Ellipses}{

If \code{show_ellipse = TRUE} (the default), a 95\% confidence ellipse is drawn
for each community using \code{\link[ggplot2:stat_ellipse]{ggplot2::stat_ellipse()}}. Ellipses require at
least 3 samples per community to be drawn; communities with fewer samples
are silently skipped.
}

\examples{
\dontrun{
data <- get_example_data()
fit  <- eDNA_dmm(data$counts, data$covariates, K = 3)

# Default NMDS (2D, Bray-Curtis, eDNA index)
result <- eDNA_dmm_nmds(fit)
result$plot

# Access stress value
message("NMDS stress: ", round(result$nmds$stress, 3))

# 3D NMDS, view axes 1 and 3
result3d <- eDNA_dmm_nmds(fit, k = 3, nmds_axes = c(1, 3))
result3d$plot

# Custom colors and no ellipses
eDNA_dmm_nmds(
  fit,
  show_ellipse     = FALSE,
  community_colors = c("Community 1" = "#E63946",
                       "Community 2" = "#1D3557",
                       "Community 3" = "#A8DADC")
)$plot

# Save the plot
ggplot2::ggsave("nmds.png", result$plot, width = 7, height = 6, dpi = 300)
}

}
\seealso{
\code{\link[=eDNA_dmm]{eDNA_dmm()}}, \code{\link[=eDNA_dmm_structure]{eDNA_dmm_structure()}}, \code{\link[=eDNA_dmm_beta]{eDNA_dmm_beta()}},
\code{\link[vegan:metaMDS]{vegan::metaMDS()}}
}
