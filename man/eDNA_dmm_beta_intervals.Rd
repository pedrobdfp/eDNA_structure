% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eDNA_dmm_beta_intervals.R
\name{eDNA_dmm_beta_intervals}
\alias{eDNA_dmm_beta_intervals}
\title{Plot covariate effects as posterior intervals}
\usage{
eDNA_dmm_beta_intervals(
  fit,
  covariates_to_plot = NULL,
  show_intercept = FALSE,
  intervals = c(0.5, 0.9),
  reference = "none",
  point_est = c("median", "mean"),
  community_colors = NULL,
  color_by_community = TRUE,
  facet_scales = "free_x",
  point_size = 2.8,
  linewidth_inner = 1.6,
  linewidth_outer = 0.6,
  base_size = 13,
  title = NULL,
  subtitle = NULL
)
}
\arguments{
\item{fit}{An \code{edna_dmm_fit} object from \code{\link[=eDNA_dmm]{eDNA_dmm()}}.}

\item{covariates_to_plot}{Character vector of covariate names to show, or
\code{NULL} (default) for all of them.}

\item{show_intercept}{Logical. Include the intercept coefficients.
Default \code{FALSE}.}

\item{intervals}{A length-2 numeric vector giving the inner (thick) and
outer (thin) interval widths, each strictly between 0 and 1. Default
\code{c(0.5, 0.9)}. Order does not matter; the wider one is drawn thin.}

\item{reference}{How the coefficients are identified for display.
\code{"none"} (default) centres them across communities, so each
value is that community's deviation from the average community and all
\code{K} communities are drawn. An integer instead reads every
coefficient as a contrast against that community, which is then omitted
as the reference. \code{NULL} uses the fit's own \code{beta_reference} if it has one.

Changing this requires no refitting. The softmax is invariant to adding
a constant across communities, so the choice is a normalisation applied
to the finished draws, not an estimate.}

\item{point_est}{One of \code{"median"} (default) or \code{"mean"}: the posterior
summary the point marks.}

\item{community_colors}{Named character vector of colors, or \code{NULL}
(default) to use the package palette. Points and lines are colored by
community; no legend is drawn because the y axis already names them.}

\item{color_by_community}{Logical. Color rows by community. Default \code{TRUE}.
\code{FALSE} draws everything in a single dark color.}

\item{facet_scales}{Passed to \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}. Default \code{"free_x"},
so each covariate gets its own x range. Use \code{"fixed"} to compare
magnitudes across covariates on a common scale.}

\item{point_size}{Size of the point estimate marker. Default \code{2.8}.}

\item{linewidth_inner, linewidth_outer}{Line widths of the inner and outer
intervals. Defaults \code{1.6} and \code{0.6}.}

\item{base_size}{Base font size. Default \code{13}.}

\item{title, subtitle}{Plot title and subtitle, or \code{NULL}.}
}
\value{
A list with two elements:
\describe{
\item{\code{plot}}{A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.}
\item{\code{table}}{A data frame with one row per coefficient: \code{community},
\code{covariate}, \code{estimate}, the four interval bounds, and \code{excludes_zero}.}
}
}
\description{
Draws the softmax regression coefficients (beta) as a coefficient plot: one
row per community and covariate, a point at the posterior point estimate,
two nested credible intervals, and a dashed line at zero.

This is a companion to \code{\link[=eDNA_dmm_beta]{eDNA_dmm_beta()}}, which overlays prior and posterior
densities. Densities show how far the data moved a coefficient from its
prior, but the tail of a density is hard to read against zero. This function
answers the complementary question directly: does the interval cross zero,
and by how much.
}
\section{Reading the plot}{

Each row is one coefficient, relative to the reference community:
\itemize{
\item The \strong{thick line} is the inner interval (50\% by default).
\item The \strong{thin line} is the outer interval (90\% by default).
\item The \strong{point} is filled when the outer interval excludes zero and hollow
when it crosses zero.
}

A coefficient whose outer interval lies entirely above zero means the
community becomes more likely as the covariate increases, relative to the
reference community; entirely below zero means less likely.
}

\examples{
\dontrun{
fit <- eDNA_dmm(counts, covariates, K = 3)

# Default 50\% and 90\% intervals
eDNA_dmm_beta_intervals(fit)$plot

# 50\% and 95\% instead
eDNA_dmm_beta_intervals(fit, intervals = c(0.5, 0.95))$plot
}

}
\seealso{
\code{\link[=eDNA_dmm_beta]{eDNA_dmm_beta()}} for the prior-versus-posterior density version.
}
