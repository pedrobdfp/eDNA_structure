% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dmm_beta_reference.R
\name{dmm_beta_draws}
\alias{dmm_beta_draws}
\title{Posterior draws of the softmax coefficients, against any reference community}
\usage{
dmm_beta_draws(fit, reference = NULL, n_draws = NULL)
}
\arguments{
\item{fit}{An \code{edna_dmm_fit} object from \code{\link[=eDNA_dmm]{eDNA_dmm()}}.}

\item{reference}{Integer community index to use as the reference, or \code{NULL}
(default) to keep the fitted one (community \code{K}).}

\item{n_draws}{Maximum number of posterior draws to return, or \code{NULL}
(default) for all of them.}
}
\value{
A numeric array with dimensions \verb{draws x K x (P+1)}. The second
dimension is named \verb{"Community 1" ... "Community K"}, the third
\code{c("intercept", fit$covariate_names)}. The reference community's slice is
exactly zero.
}
\description{
Returns the full \verb{K x (P+1)} coefficient matrix for every posterior draw,
including the reference community's row of zeros, optionally re-expressed
against a different reference community.

Every beta coefficient is a \strong{contrast}: community \code{k} relative to the
reference. \code{\link[=eDNA_dmm]{eDNA_dmm()}} fixes the last community as the reference, which is
an arbitrary choice made for identifiability. Which community you want to
compare against is an interpretive decision, and it usually differs from the
one the sampler happened to number last — the clearest reading often comes
from referencing the most distinct community, so that every other
coefficient states a contrast with it.

Changing the reference requires \strong{no refitting}. A softmax is invariant to
adding a constant to all communities within a covariate, so the reference is
a normalisation rather than an estimate: subtracting community \code{j}'s row from
every row moves the zero and leaves the model untouched. Doing it on the
draws rather than on the summaries means the intervals carry their
uncertainty across correctly.
}
\examples{
\dontrun{
fit <- eDNA_dmm(counts, covariates, K = 4)

# Coefficients against community 2 instead of the fitted reference
d <- dmm_beta_draws(fit, reference = 2)
dim(d)                       # draws x 4 x (P+1)
colMeans(d[, , "depth"])     # posterior mean depth effect per community

# The plotting functions take the same argument
eDNA_dmm_beta_intervals(fit, reference = 2)$plot
}

}
\seealso{
\code{\link[=eDNA_dmm_beta]{eDNA_dmm_beta()}} and \code{\link[=eDNA_dmm_beta_intervals]{eDNA_dmm_beta_intervals()}}, which both take
a \code{reference} argument and use this internally.
}
