% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dmm_beta_reference.R
\name{dmm_beta_draws}
\alias{dmm_beta_draws}
\title{Posterior draws of the softmax coefficients, against any reference community}
\usage{
dmm_beta_draws(fit, reference = NULL, n_draws = NULL)
}
\arguments{
\item{fit}{An \code{edna_dmm_fit} object from \code{\link[=eDNA_dmm]{eDNA_dmm()}}.}

\item{reference}{Integer community index to re-express the coefficients
against (that community's row becomes exactly zero), or \code{NULL} (default)
to keep the fitted centred parameterization, in which every community
has a real, generally nonzero coefficient.}

\item{n_draws}{Maximum number of posterior draws to return, or \code{NULL}
(default) for all of them.}
}
\value{
A numeric array with dimensions \verb{draws x K x (P+1)}. The second
dimension is named \verb{"Community 1" ... "Community K"}, the third
\code{c("intercept", fit$covariate_names)}. With \code{reference} set, that
community's slice is exactly zero; otherwise every slice generally has a
nonzero value (the centred parameterization has no fixed-zero row).
}
\description{
Returns the full \verb{K x (P+1)} coefficient matrix for every posterior draw,
by default in the \strong{centred} parameterization \code{\link[=eDNA_dmm]{eDNA_dmm()}} actually fits
(each community's coefficient is its deviation from the average community,
nothing is fixed at zero), optionally re-expressed as a contrast against a
chosen reference community instead.

The softmax is invariant to adding a constant to all communities within a
covariate, so centred and reference-coded views are the same model in
different coordinates. Passing \code{reference} re-expresses every coefficient
as community \code{k} relative to that one community, which then becomes exactly
zero (contrast coding). This is sometimes an easier reading than the
default deviation-from-average view, e.g. contrasting every community
against the most distinct one.

Changing the reference requires \strong{no refitting}: subtracting community
\code{j}'s row from every row moves the zero and leaves the model untouched.
Doing it on the draws rather than on the summaries means the intervals
carry their uncertainty across correctly.
}
\examples{
\dontrun{
fit <- eDNA_dmm(counts, covariates, K = 4)

# Coefficients against community 2 instead of the fitted reference
d <- dmm_beta_draws(fit, reference = 2)
dim(d)                       # draws x 4 x (P+1)
colMeans(d[, , "depth"])     # posterior mean depth effect per community

# The plotting functions take the same argument
eDNA_dmm_beta_intervals(fit, reference = 2)$plot
}

}
\seealso{
\code{\link[=eDNA_dmm_beta]{eDNA_dmm_beta()}} and \code{\link[=eDNA_dmm_beta_intervals]{eDNA_dmm_beta_intervals()}}, which both take
a \code{reference} argument and use this internally.
}
