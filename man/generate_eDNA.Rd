% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{generate_eDNA}
\alias{generate_eDNA}
\title{Simulate eDNA shedding, environmental decay, and bottle sub-sampling}
\usage{
generate_eDNA(
  contributors_list,
  shedding_error = 0,
  decay_rate = 0.1,
  shedding_rate = 1,
  beta = 0.75,
  bio_reps = 1,
  bottle_volume = 0.1
)
}
\arguments{
\item{contributors_list}{A list of contributor data frames from
\code{\link[=generate_contributors]{generate_contributors()}} (the \verb{$contributors_list} element).}

\item{shedding_error}{A non-negative number: standard deviation of lognormal
multiplicative noise on per-contributor shedding. \code{0} = no noise (default).}

\item{decay_rate}{A positive number: exponential decay rate with distance.
Default \code{0.1}. Higher values = eDNA signal drops off faster with distance.}

\item{shedding_rate}{A positive number: baseline shedding multiplier.
Default \code{1}. Scale up (e.g., \code{1000}) to get more molecules and thus
more stable sub-sampling.}

\item{beta}{A positive number: allometric exponent relating body size to
eDNA shedding. Default \code{0.75} (standard metabolic scaling). \code{1.0} = linear.}

\item{bio_reps}{A positive integer: number of independent bottle sub-samples
drawn from each local eDNA pool. Default \code{1}.}

\item{bottle_volume}{A number in (0, 1]: fraction of the local eDNA pool
captured in each bottle. Default \code{0.1}.}
}
\value{
A list with:
\describe{
\item{\code{Summary}}{Data frame with one row per (sample, bio_rep) combination:
\code{SampleID}, \code{TrueCommunity}, \code{BioRep}, \code{Total_eDNA}, \code{Total_contributors},
\code{Avg_distance}, \code{Closest_contributor}, \code{SD_distance}.}
\item{\code{Species_frequencies}}{Data frame with relative species frequencies
per (sample, bio_rep): \code{SampleID}, \code{TrueCommunity}, \code{BioRep}, \code{Species},
\code{Relative_frequency}.}
}
}
\description{
Each contributor sheds eDNA proportional to its body size raised to an
allometric exponent, with optional lognormal multiplicative noise. eDNA
decays exponentially with distance to the sampler. The resulting molecular
pool is sub-sampled (with replacement) to represent one or more biological
replicates (bottles).
}
\seealso{
\code{\link[=generate_contributors]{generate_contributors()}}, \code{\link[=simulate_metabarcoding]{simulate_metabarcoding()}}
}
