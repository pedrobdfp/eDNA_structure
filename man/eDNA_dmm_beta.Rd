% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eDNA_dmm_beta.R
\name{eDNA_dmm_beta}
\alias{eDNA_dmm_beta}
\title{Plot covariate effects: prior vs posterior distributions of beta coefficients}
\usage{
eDNA_dmm_beta(
  fit,
  layout = "joint",
  covariates_to_plot = NULL,
  show_intercept = FALSE,
  beta_prior_sd = 1,
  n_prior_samples = 20000,
  community_colors = NULL,
  prior_color = "grey60",
  prior_alpha = 0.35,
  posterior_alpha = 0.55,
  show_annotations = NULL,
  base_size = 13,
  title = NULL,
  subtitle = NULL
)
}
\arguments{
\item{fit}{An \code{edna_dmm_fit} object from \code{\link[=eDNA_dmm]{eDNA_dmm()}}.}

\item{layout}{A string: \code{"joint"} (default) or \code{"separate"}. Controls
whether communities are overlaid or in separate facet rows. See the
\strong{Joint vs separate layout} section above.}

\item{covariates_to_plot}{A character vector of covariate names to include.
Default is \code{NULL}, which plots all covariates (excluding the intercept).
Example: \code{covariates_to_plot = c("depth", "latitude")}.}

\item{show_intercept}{Logical. If \code{FALSE} (the default), the intercept
coefficient is excluded from the plot (it is rarely of direct interest).
Set to \code{TRUE} to include it.}

\item{beta_prior_sd}{A positive number: the standard deviation of the
Normal(0, \code{beta_prior_sd}) prior used on beta coefficients in the Stan
model. Default is \code{1.0}. This \strong{must match} the \code{to_vector(beta) ~ normal(0, sd)}
line in the Stan model. The default Stan model uses \code{normal(0, 1.0)}. If
you have changed the prior (e.g., to \code{normal(0, 2.5)}), update this argument.}

\item{n_prior_samples}{A positive integer: the number of draws from the
prior distribution to use for the prior density. Default is \code{20000}.
Higher values give smoother prior curves but make no difference to the
posterior.}

\item{community_colors}{A named character vector mapping community names
to hex colors. Pass \code{NULL} (the default) for the automatic HCL palette.}

\item{prior_color}{A string: the fill color for the prior density.
Default is \code{"grey60"}.}

\item{prior_alpha}{A number in (0, 1]: transparency of the prior density.
Default is \code{0.35}.}

\item{posterior_alpha}{A number in (0, 1]: transparency of the posterior
density. Default is \code{0.55}.}

\item{show_annotations}{Logical. If \code{TRUE} (the default), annotate each
panel with the posterior mean, 90\% CI, and P(direction) value.}

\item{base_size}{A positive number: the base font size. Default is \code{13}.}

\item{title}{A string: the plot title. Default is auto-generated.}

\item{subtitle}{A string: the plot subtitle. Default is auto-generated.}
}
\value{
A list with two elements:
\describe{
\item{\code{plot}}{A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.}
\item{\code{table}}{A data frame with posterior summaries for each coefficient.
Columns: \code{community}, \code{covariate}, \code{mean}, \code{median}, \code{ci_5}, \code{ci_95},
\code{prob_positive}, \code{prob_negative}, \code{ess}, \code{reliability}.}
}
}
\description{
Produces a prior-versus-posterior density plot for the softmax regression
coefficients (beta) that describe how environmental covariates drive
community membership. A posterior distribution that is pulled away from the
prior is evidence that a covariate genuinely predicts community membership.
A posterior that overlaps heavily with the prior means the data contain
little information about that covariate's effect.

The function returns a \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\section{Joint vs separate layout}{

The \code{layout} argument controls how communities are displayed:
\itemize{
\item \code{"joint"} (default): all communities overlaid on the same panel per
covariate, each community a different color. Good for comparing community
effects at a glance.
\item \code{"separate"}: one row per community (facet rows), one column per
covariate (facet columns). Better when you want to inspect each community
in detail without overlap.
}
}

\section{Reading the plot}{

For each covariate:
\itemize{
\item \strong{Grey}: the prior distribution — Normal(0, \code{beta_prior_sd}). This is
what the model assumed before seeing the data.
\item \strong{Colored}: the posterior distribution — what the model learned from the
data. One color per community.
}

Interpret:
\itemize{
\item \strong{Posterior >> 0}: this community is more likely at high values of the
covariate (relative to the reference community).
\item \strong{Posterior << 0}: this community is less likely at high values of the
covariate.
\item \strong{Posterior ≈ prior}: the data do not constrain this coefficient; the
covariate may not predict community membership.
}
}

\section{Coefficient table}{

The function also prints and invisibly returns a data frame of posterior
summaries (mean, 90\% CI, probability of direction, ESS, reliability). To
access it:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{result <- eDNA_dmm_beta(fit)
result$table
}\if{html}{\out{</div>}}
}

\examples{
\dontrun{
data <- get_example_data()
fit  <- eDNA_dmm(data$counts, data$covariates, K = 3)

# Default: joint layout, all covariates, prior SD = 1
result <- eDNA_dmm_beta(fit)
result$plot
result$table

# Separate layout (each community in its own row)
eDNA_dmm_beta(fit, layout = "separate")$plot

# Only plot specific covariates
eDNA_dmm_beta(fit, covariates_to_plot = c("depth"))$plot

# If you changed the beta prior in Stan to normal(0, 2.5):
eDNA_dmm_beta(fit, beta_prior_sd = 2.5)$plot
}

}
\seealso{
\code{\link[=eDNA_dmm]{eDNA_dmm()}}, \code{\link[=eDNA_dmm_structure]{eDNA_dmm_structure()}}, \code{\link[=eDNA_dmm_nmds]{eDNA_dmm_nmds()}}
}
