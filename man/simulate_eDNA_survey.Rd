% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_eDNA_survey}
\alias{simulate_eDNA_survey}
\title{Run the full eDNA survey simulation pipeline in one call}
\usage{
simulate_eDNA_survey(
  n_communities = 4,
  n_species = 40,
  samples_per_community = 3,
  community_covariate_means = NULL,
  covariate_sds = NULL,
  mean_read_depth = 10000,
  bio_reps = 1,
  seq_reps = 1,
  spillover = 0.15,
  shedding_error = 0.3,
  decay_rate = 0.1,
  seed = 42,
  ...
)
}
\arguments{
\item{n_communities}{Integer. Number of communities K. Default \code{4}.}

\item{n_species}{Integer. Number of species S. Default \code{40}.}

\item{samples_per_community}{Integer. Default \code{3}.}

\item{community_covariate_means}{A \verb{K × P} matrix of covariate means per
community. Rows = communities, columns = covariates. If \code{NULL} (default),
a two-covariate (Depth × Distance_shore) design with four well-separated
communities is used.}

\item{covariate_sds}{A \verb{K × P} matrix of covariate SDs, or \code{NULL} for
SD = 5 on all. Default \code{NULL}.}

\item{mean_read_depth}{Numeric. Mean reads per sample. Default \code{10000}.}

\item{bio_reps}{Integer. Biological replicates per sample. Default \code{1}.}

\item{seq_reps}{Integer. Sequencing technical replicates per bio rep.
Default \code{1}.}

\item{spillover}{Numeric \verb{[0,1]}. Community spillover. Default \code{0.15}.}

\item{shedding_error}{Numeric. Lognormal shedding noise SD. Default \code{0.3}.}

\item{decay_rate}{Numeric. eDNA decay rate with distance. Default \code{0.1}.}

\item{seed}{Integer random seed. Default \code{42}.}

\item{...}{Additional arguments passed to \code{\link[=generate_community_compositions]{generate_community_compositions()}}.}
}
\value{
A list. The first three elements mirror \code{\link[=get_example_data]{get_example_data()}} exactly,
so code written against one works unchanged against the other:
\describe{
\item{\code{counts}}{Integer matrix \verb{N x S}, samples in rows, ready for
\code{\link[=eDNA_dmm]{eDNA_dmm()}}.}
\item{\code{covariates}}{Data frame of \strong{numeric covariate columns only}, one
row per sample, ready to pass straight to \code{\link[=eDNA_dmm]{eDNA_dmm()}}. Identifier and
ground-truth columns are deliberately excluded: \code{\link[=eDNA_dmm]{eDNA_dmm()}} fits every
column it is given, so a \code{sample_id} would be fitted as though it were
an environmental gradient.}
\item{\code{metadata}}{Data frame with \code{sample_id}, \code{TrueCommunity} and the
covariate columns - the labelling columns, for the \code{metadata} argument
of \code{\link[=plot_true_compositions]{plot_true_compositions()}} and \code{\link[=eDNA_dmm_structure]{eDNA_dmm_structure()}}.}
}
The remaining elements are the simulation's ground truth, for checking
parameter recovery:
\describe{
\item{\code{community_compositions}}{The true \verb{K x S} composition matrix.}
\item{\code{metab_df}}{Raw metabarcoding data frame.}
\item{\code{sample_metadata}}{Full sample metadata data frame.}
\item{\code{contributors}}{Output of \code{\link[=generate_contributors]{generate_contributors()}}.}
}
}
\description{
Convenience wrapper that runs all five simulation steps in sequence:
\code{\link[=generate_community_compositions]{generate_community_compositions()}}, \code{\link[=generate_contributors]{generate_contributors()}},
\code{\link[=generate_eDNA]{generate_eDNA()}}, \code{\link[=simulate_metabarcoding]{simulate_metabarcoding()}}, and
\code{\link[=generate_sample_covariates]{generate_sample_covariates()}}. Returns all intermediate outputs plus
a ready-to-use count matrix and covariate data frame formatted for
\code{\link[=eDNA_dmm]{eDNA_dmm()}}.
}
\examples{
\dontrun{
sim <- simulate_eDNA_survey(
  n_communities         = 3,
  n_species             = 30,
  samples_per_community = 5,
  seed                  = 42
)
dim(sim$counts)       # N x 30
head(sim$covariates)

fit <- eDNA_dmm(sim$counts, sim$covariates[, c("Depth", "Distance_shore")], K = 3)
}

}
\seealso{
\code{\link[=eDNA_dmm]{eDNA_dmm()}}, \code{\link[=generate_community_compositions]{generate_community_compositions()}},
\code{\link[=plot_true_compositions]{plot_true_compositions()}}
}
