% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{example_edna}
\alias{example_edna}
\title{Example eDNA metabarcoding dataset}
\format{
A named list with three elements:
\describe{
\item{\code{counts}}{Integer matrix, 20 sites x 32 taxa. Rows are sites
(\code{STN_001}-\code{STN_020}), columns are taxa (\code{Sp_1}, \code{Sp_2}, ...).}
\item{\code{covariates}}{Data frame, 20 rows x 2 numeric columns: \code{Depth} (m)
and \code{Distance_shore}. Numeric only, so it can be passed directly to
\code{\link[=eDNA_dmm]{eDNA_dmm()}}.}
\item{\code{metadata}}{Data frame, 20 rows: \code{sample_id}, \code{TrueCommunity},
\code{Depth}, \code{Distance_shore}. The labelling columns, for the plotting
functions' \code{metadata} argument.}
}
}
\source{
Generated by \code{\link[=simulate_eDNA_survey]{simulate_eDNA_survey()}} with \code{seed = 42}.
See \code{data-raw/generate_example_data.R} for the exact call. Only the fields
a user needs are shipped; the simulation's internal tables are not.
}
\description{
A small simulated eDNA survey with known ground truth: 20 sites, 32 taxa,
four communities separated by depth and distance from shore. Use it to learn
the package, follow the vignettes (\code{vignette("tutorial")}), and verify your
installation.

Load it via \code{\link[=get_example_data]{get_example_data()}}.
}
\section{Community structure}{

Four communities separated by two covariates:
\itemize{
\item \strong{Community 1}: deep (70 m) + offshore (140 units)
\item \strong{Community 2}: shallow (40 m) + offshore (140 units)
\item \strong{Community 3}: deep (70 m) + inshore (100 units)
\item \strong{Community 4}: shallow (40 m) + inshore (100 units)
}
Each covariate has SD 6-8 m/units within a community: enough for a clean
visual separation without making the covariates (quasi-)perfectly
separating, which would starve the softmax regression coefficients of
gradient and produce poor chain mixing.

Spillover = 0.15 (15\% of frequency leaks between communities).
Overdispersion alpha = ~3.
}

\examples{
d <- get_example_data()
dim(d$counts)          # 20 x 32
head(d$covariates)

}
\keyword{datasets}
