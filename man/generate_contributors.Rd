% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{generate_contributors}
\alias{generate_contributors}
\title{Generate eDNA contributors for each simulated sample}
\usage{
generate_contributors(
  community_compositions,
  samples_per_community = 3,
  n_contributors_range = c(20, 200),
  grouped_species_vec = NULL,
  size_range = c(1, 10),
  distance_range = c(0, 100),
  group_size_range = c(2, 10),
  n_distribution = c("Negative Binomial", "Random"),
  seed = NULL
)
}
\arguments{
\item{community_compositions}{A \verb{K x S} community composition matrix from
\code{\link[=generate_community_compositions]{generate_community_compositions()}}.}

\item{samples_per_community}{A positive integer: number of independent samples
(stations) drawn from each community. Default \code{3}.}

\item{n_contributors_range}{A length-2 integer vector: min and max number of
individual organisms contributing eDNA per sample. Default \code{c(20, 200)}.}

\item{grouped_species_vec}{A logical vector of length S: \code{TRUE} if the species
is gregarious (school members share a distance draw); \code{FALSE} for solitary
(independent distances). Pass \code{NULL} (default) for all species solitary.}

\item{size_range}{A length-2 numeric vector: min and max organism body size
(arbitrary units, affects eDNA shedding). Default \code{c(1, 10)}.}

\item{distance_range}{A length-2 numeric vector: min and max distance from
organism to sampler (same units as \code{decay_rate} in \code{\link[=generate_eDNA]{generate_eDNA()}}).
Default \code{c(0, 100)}.}

\item{group_size_range}{A length-2 integer vector: min and max school size
for grouped species. Default \code{c(2, 10)}.}

\item{n_distribution}{A string: \code{"Random"} (uniform between
\code{n_contributors_range}) or \code{"Negative Binomial"} (overdispersed, more
realistic). Default \code{"Negative Binomial"}.}

\item{seed}{An integer random seed, or \code{NULL}. Default \code{NULL}.}
}
\value{
A list with elements:
\describe{
\item{\code{contributors_list}}{A list of data frames, one per sample, each with
columns \code{SampleID}, \code{TrueCommunity}, \code{ContributorID}, \code{Species},
\code{Distance}, \code{Size}.}
\item{\code{community_compositions}}{The input composition matrix (passed through).}
\item{\code{grouped_species_vec}}{The logical grouping vector (passed through).}
}
}
\description{
For each community x sample replicate combination, draws a set of individual
organisms (contributors) whose species are sampled from the community's
frequency vector. Each contributor is assigned a body size and a distance
from the sampler. Species flagged as grouped (schooling) share distances
within groups; ungrouped (solitary) species get independent distances.
}
\examples{
cm   <- generate_community_compositions(n_communities = 2, n_species = 20, seed = 1)
cont <- generate_contributors(cm, samples_per_community = 3, seed = 1)
length(cont$contributors_list)   # 6 samples (2 communities x 3)

}
\seealso{
\code{\link[=generate_community_compositions]{generate_community_compositions()}}, \code{\link[=generate_eDNA]{generate_eDNA()}}
}
