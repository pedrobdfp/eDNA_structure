% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{example_edna_reps}
\alias{example_edna_reps}
\title{Example eDNA dataset with replicates}
\format{
A named list with four elements:
\describe{
\item{\code{counts}}{Integer matrix, 60 replicates x 33 taxa. Row names are
\code{STN_001_B1}, \code{STN_001_B2}, ... - three replicates per site.}
\item{\code{replication}}{Character vector of length 60 giving the site each row
belongs to. Pass as \code{replication} to \code{\link[=eDNA_dmm]{eDNA_dmm()}}.}
\item{\code{covariates}}{Data frame, 20 rows x 2 numeric columns. Covariates are
station-level: one row per site, not per replicate.}
\item{\code{metadata}}{Data frame, 20 rows, station-level labelling columns.}
}
}
\source{
Generated by \code{\link[=simulate_eDNA_survey]{simulate_eDNA_survey()}} with \code{seed = 2026} and
\code{bio_reps = 3}. See \code{data-raw/generate_example_data.R}.
}
\description{
The same survey as \link{example_edna}, but with three replicates per site
instead of one pooled sample. Shipped so the expected shape of replicated
data is visible without having to simulate it first.

Load it via \code{\link[=get_example_replicates]{get_example_replicates()}}.
}
\examples{
r <- get_example_replicates()
dim(r$counts)           # 60 x 33
head(r$replication, 6)

}
\keyword{datasets}
