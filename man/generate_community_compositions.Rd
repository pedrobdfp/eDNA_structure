% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{generate_community_compositions}
\alias{generate_community_compositions}
\title{Generate community composition matrices for eDNA simulation}
\usage{
generate_community_compositions(
  n_communities = 4,
  n_species = 50,
  n_shared = NULL,
  spillover = 0.15,
  spillover_concentration = 10,
  community_groups = NULL,
  n_group_shared = 0,
  group_shared_freq = c(0.02, 0.08),
  group_shared_presence = 0.9,
  n_dominant_range = c(2, 3),
  n_unique_low_freq = 3,
  dominant_freq = c(0.15, 0.3),
  low_unique_freq = c(0.005, 0.04),
  shared_freq = c(0.001, 0.05),
  shared_presence = 0.4,
  seed = NULL
)
}
\arguments{
\item{n_communities}{A positive integer: number of communities (K). Default \code{4}.}

\item{n_species}{A positive integer: total number of species (S). Default \code{50}.}

\item{n_shared}{An integer or NULL: number of species in the shared background
pool. NULL (default) uses all remaining species after dominants, uniques,
and group-shared are assigned.}

\item{spillover}{A number in \verb{[0, 1]}: mean fraction of a species' total
frequency that leaks into non-native communities. Default \code{0.15}.
\code{0} = perfectly exclusive communities; \code{1} = no community structure.}

\item{spillover_concentration}{A positive number: concentration of the Beta
prior governing per-species spillover variance. Higher values force all
species to have spillover close to the mean. Default \code{10}.}

\item{community_groups}{An integer vector of length K assigning communities
to groups (e.g., \code{c(1, 2, 1, 2)} means communities 1 and 3 share a group).
Group-shared species appear in all communities of the same group but not
others. Pass \code{NULL} (default) for no group structure.}

\item{n_group_shared}{A non-negative integer: number of species shared within
each group (not across groups). Default \code{0}.}

\item{group_shared_freq}{A length-2 numeric vector: min and max frequency for
group-shared species. Default \code{c(0.02, 0.08)}.}

\item{group_shared_presence}{A number in \verb{[0, 1]}: probability that a
group-shared species is present in a given community of its group.
Default \code{0.9}.}

\item{n_dominant_range}{A length-2 integer vector: min and max number of
dominant species per community. Default \code{c(2, 3)}.}

\item{n_unique_low_freq}{A non-negative integer: number of unique low-frequency
species per community. Default \code{3}.}

\item{dominant_freq}{A length-2 numeric vector: min and max frequency for
dominant species. Default \code{c(0.15, 0.30)}.}

\item{low_unique_freq}{A length-2 numeric vector: min and max frequency for
unique low-frequency species. Default \code{c(0.005, 0.04)}.}

\item{shared_freq}{A length-2 numeric vector: min and max frequency for
shared background species. Default \code{c(0.001, 0.05)}.}

\item{shared_presence}{A number in \verb{[0, 1]}: probability that a shared
background species appears in a given community. Default \code{0.4}.}

\item{seed}{An integer random seed for reproducibility, or \code{NULL}. Default \code{NULL}.}
}
\value{
A numeric matrix \verb{K x S} of community compositions (rows sum to 1).
Row names are \code{"Community_1"} through \code{"Community_K"}. Column names are
\code{"Sp_1"} through \code{"Sp_S"}. The matrix carries attributes documenting which
species are dominants, uniques, group-shared, shared, and absent per community.
}
\description{
Builds K community compositions over S species. Each community has a set of
dominant species (high frequency, mostly exclusive to that community),
unique low-frequency species (present only in that community), optional
group-shared species (shared within a set of related communities), and a
shared background pool. Spillover parameters control how much a species'
frequency leaks into non-native communities.
}
\examples{
cm <- generate_community_compositions(
  n_communities = 3,
  n_species     = 30,
  seed          = 42
)
dim(cm)       # 3 x 30
rowSums(cm)   # all 1

}
\seealso{
\code{\link[=generate_contributors]{generate_contributors()}}, \code{\link[=simulate_eDNA_survey]{simulate_eDNA_survey()}}
}
