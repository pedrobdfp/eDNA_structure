% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_metabarcoding}
\alias{simulate_metabarcoding}
\title{Simulate metabarcoding sequencing from eDNA frequencies}
\usage{
simulate_metabarcoding(
  eDNA_data,
  mean_read_depth = 10000,
  read_depth_sd = 0.2,
  error_sd = 0.1,
  rep = 1,
  amplification_bias = NULL
)
}
\arguments{
\item{eDNA_data}{The output of \code{\link[=generate_eDNA]{generate_eDNA()}}.}

\item{mean_read_depth}{A positive number: mean total reads per sample.
Default \code{10000}.}

\item{read_depth_sd}{A non-negative number: standard deviation of the
lognormal distribution for read depth. Default \code{0.2}.}

\item{error_sd}{A non-negative number: standard deviation of Gaussian noise
added to species frequencies before multinomial sampling. Default \code{0.1}.}

\item{rep}{A positive integer: number of sequencing technical replicates per
biological replicate. Default \code{1}.}

\item{amplification_bias}{A numeric vector of length S (number of species):
per-species amplification multipliers. Values > 1 inflate that species'
apparent frequency; values < 1 deflate it. Pass \code{NULL} (default) for
unbiased amplification.}
}
\value{
A data frame with columns: \code{SampleID}, \code{BioRep}, \code{SeqRep}, \code{Species}
(integer index), \code{Counts}.
}
\description{
Applies optional per-species amplification bias to the bottle's species
frequencies, adds symmetric Gaussian noise, renormalizes, then draws read
counts from a multinomial distribution with lognormally-variable read depth.
}
\seealso{
\code{\link[=generate_eDNA]{generate_eDNA()}}, \code{\link[=simulate_eDNA_survey]{simulate_eDNA_survey()}}
}
