% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eDNA_dmm.R
\name{.print_hmc_diagnostics}
\alias{.print_hmc_diagnostics}
\title{Print HMC diagnostics without relying on rstan's own message()-based output}
\usage{
.print_hmc_diagnostics(stan_fit)
}
\arguments{
\item{stan_fit}{An \code{rstan::stanfit} object.}
}
\value{
Invisibly \code{NULL}, called for its printed side effect.
}
\description{
\code{rstan::check_hmc_diagnostics()} prints its header lines ("Divergences:",
"Tree depth:", "Energy:") with \code{cat()}, but the actual counts on each
("0 of 4000 iterations...") with \code{message()}. Those go to a different
stream (the R condition system, not stdout), and are silently dropped by
some consoles/terminals/IDE integrations that do show \code{cat()}/\code{print()}
output, which produces exactly this: three headers with nothing under any
of them. Every other line this package prints (print/summary output) is
\code{cat()}-based and visible everywhere, so this rebuilds the same three
checks the same way, using rstan's own (exported) accessors for the
underlying numbers.
}
\keyword{internal}
