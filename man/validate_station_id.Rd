% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_station_id}
\alias{validate_station_id}
\title{Validate a station_id vector against a replicate-level count matrix}
\usage{
validate_station_id(station_id, counts, call = rlang::caller_env())
}
\value{
A list with \code{index} (integer station index per row), \code{levels},
\code{n_stations}, \code{reps} (replicates per station) and \code{fallback}.
}
\description{
Returns the information \code{eDNA_dmm()} needs to build ragged replicate data,
plus a \code{fallback} flag. \code{fallback = TRUE} means no station has more than one
replicate, in which case the replicate model's two dispersion parameters
(\code{alpha}, \code{phi}) are not separately identified — and the caller should simply
use the standard model, which is the correct model for unreplicated data.
}
\keyword{internal}
