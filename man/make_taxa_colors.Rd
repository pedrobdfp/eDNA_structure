% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{make_taxa_colors}
\alias{make_taxa_colors}
\title{Structured taxon palette: hue families, shaded within each}
\usage{
make_taxa_colors(taxa, n_hues = 7, include_other = TRUE)
}
\arguments{
\item{taxa}{Character vector of taxon names. \code{"Other"} is ignored here and
appended as grey by \code{include_other}.}

\item{n_hues}{Number of hue families. Default \code{7}.}

\item{include_other}{Append \code{Other = "grey70"}. Default \code{TRUE}.}
}
\value{
A named character vector of colours.
}
\description{
Taxa are sorted and then laid out hue block by hue block, so neighbouring
names share a hue and differ in lightness. That is what lets a legend of
thirty-odd taxa be read as a handful of colour families rather than as
thirty arbitrary swatches.
}
\details{
Counts are spread across ALL \code{n_hues} hues before any shading is added.
The earlier version built a shades-by-hues grid and read it column-major,
which meant a taxon count that the first few hues could absorb left the
last hue unused: 30 taxa became six hues of five shades and the seventh
hue, the pink one, never appeared in any figure.
}
\keyword{internal}
