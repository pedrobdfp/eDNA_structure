% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eDNA_dmm_compositions.R
\name{dmm_taxon_order}
\alias{dmm_taxon_order}
\title{Rank taxa by their weight in a fitted model}
\usage{
dmm_taxon_order(fit, top_n = NULL)
}
\arguments{
\item{fit}{An \code{edna_dmm_fit} object from \code{\link[=eDNA_dmm]{eDNA_dmm()}}.}

\item{top_n}{Number of taxa to return. \code{NULL} (default) returns all of them,
ranked.}
}
\value{
A character vector of taxon names, highest weight first.
}
\description{
Returns taxa ordered by total posterior weight across communities,
\code{colSums(pi_mean)}. A taxon that dominates a single community ranks highly
even if it is rare overall, which is the ordering that matters when the
question is what distinguishes communities from one another.

This is deliberately NOT the same ranking as mean observed frequency across
samples, which rewards being widespread instead. Use this function to drive
BOTH \code{\link[=eDNA_dmm_compositions]{eDNA_dmm_compositions()}} and \code{\link[=plot_true_compositions]{plot_true_compositions()}} when the two
figures sit side by side, because the taxon palette is a deterministic
function of the sorted taxon set: same set in, same colours out. Selecting
the two sets independently gives two different palettes, and shared taxa
then appear in different colours in the two panels.
}
\seealso{
\code{\link[=eDNA_dmm_compositions]{eDNA_dmm_compositions()}}, \code{\link[=plot_true_compositions]{plot_true_compositions()}}
}
