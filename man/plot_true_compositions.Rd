% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_true_compositions.R
\name{plot_true_compositions}
\alias{plot_true_compositions}
\title{Plot observed species composition per sample (pre-fit diagnostic)}
\usage{
plot_true_compositions(
  counts,
  metadata = NULL,
  sample_id_col = "sample_id",
  facet_var = NULL,
  facet_row_var = NULL,
  sort_var = NULL,
  top_n = 20,
  taxa_include = NULL,
  base_size = 11,
  title = NULL,
  subtitle = NULL,
  show_legend = FALSE,
  legend_position = "bottom",
  legend_nrow = NULL,
  legend_ncol = NULL,
  legend_key_size = 0.4,
  legend_text_size = NULL,
  legend_rel_height = 0.1,
  legend_rel_width = 0.2,
  n_hues = 7,
  bar_width = 0.9,
  panel_spacing = 0.3,
  ylab = "Proportion",
  strip_text_size = NULL,
  row_label_size = NULL,
  ylab_size = NULL,
  ylab_rel_width = 0.03,
  row_label_fn = as.character,
  row_label_position = c("title", "ylab"),
  panel_labels = TRUE,
  vline_var = NULL,
  vline_value = NULL,
  vline_color = "black",
  vline_linetype = "dashed",
  vline_linewidth = 0.7
)
}
\arguments{
\item{counts}{A numeric matrix or data frame of read counts (samples x taxa).}

\item{metadata}{An optional data frame with one row per sample.}

\item{sample_id_col}{Column in \code{metadata} matching \code{rownames(counts)}.
Default \code{"sample_id"}.}

\item{facet_var}{Column for column facets (e.g. \code{"year"}). Default \code{NULL}.}

\item{facet_row_var}{Column for row facets (e.g. \code{"depth_bin"}). When
supplied, one sub-plot is built per level and stacked with cowplot.
Default \code{NULL}.}

\item{sort_var}{Column to sort samples within panels. Default \code{NULL}.}

\item{top_n}{Number of most abundant taxa shown individually; rest collapsed
to \code{"Other"}. Ranked by mean observed frequency across samples. Ignored
when \code{taxa_include} is supplied. Default \code{20}.}

\item{taxa_include}{Character vector naming exactly which taxa to show
individually, everything else pooled into \code{"Other"}. Supply the result of
\code{\link[=dmm_taxon_order]{dmm_taxon_order()}} here to make this figure share a taxon set, and
therefore a colour palette, with \code{\link[=eDNA_dmm_compositions]{eDNA_dmm_compositions()}}. Names absent
from \code{counts} are dropped with a warning. Default \code{NULL}, meaning rank by
\code{top_n}.}

\item{base_size}{Base font size. Default \code{11}.}

\item{title}{Plot title. Default auto-generated.}

\item{subtitle}{Plot subtitle. Default \code{NULL}, meaning no subtitle.}

\item{show_legend}{Logical. Show the taxon legend. Default \code{FALSE} - a
full taxon legend is usually too large to be useful on this plot.}

\item{legend_position}{Legend position. Default \code{"bottom"}.}

\item{legend_nrow}{Number of legend rows, passed to \code{guide_legend()}.
Default \code{NULL} (ggplot chooses).}

\item{legend_ncol}{Number of legend columns, passed to \code{guide_legend()}.
Default \code{NULL} (ggplot chooses).}

\item{legend_key_size}{Size (cm) of each legend color swatch. Default \code{0.4}.}

\item{legend_text_size}{Font size of legend labels. Default \code{NULL}
(inherits from \code{base_size}).}

\item{legend_rel_height}{Height of a top or bottom legend, as a fraction of
the panel stack. Default \code{0.1}. Raise it when the legend has many rows:
the legend is given exactly this much room, and rows that do not fit are
clipped without warning.}

\item{legend_rel_width}{Width of a left or right legend, as a fraction of
the panel stack. Default \code{0.2}.}

\item{n_hues}{Number of hue families in the taxon palette. Taxa are sorted
and laid out hue block by hue block, so a legend of \code{n_hues} columns puts
roughly one colour family per column. Default \code{7}.}

\item{bar_width}{Width of each bar, in units of one x slot. Default \code{0.9},}

\item{panel_spacing}{Numeric. Horizontal gap between facet panels, in
lines. Default \code{0.3}. Increase it when panels hold few bars, where the
default reads as narrower than the gaps between the bars themselves.
which leaves a thin white gutter so individual samples stay countable.
Use \code{1} for a gapless STRUCTURE-style block.}

\item{ylab}{Shared y-axis label drawn once beside the panel stack, used only
when \code{row_label_position = "title"}. Default \code{"Proportion"}.}

\item{strip_text_size}{Font size of the column-facet strip labels (e.g. the
years). Default \code{NULL}, meaning \code{base_size}.}

\item{row_label_size}{Font size of the per-row label produced by
\code{row_label_fn} (e.g. the depths), when \code{row_label_position = "title"}.
Default \code{NULL}, meaning \code{base_size}.}

\item{ylab_size}{Font size of the shared y-axis label. Default \code{NULL},
meaning \code{base_size}.}

\item{ylab_rel_width}{Width of that label strip, as a fraction of the panel
stack. Default \code{0.03}.}

\item{row_label_fn}{Function applied to each \code{facet_row_var} level to
produce its display label, e.g. \code{function(x) paste0(x, " m")} for a
depth column, or \code{function(x) paste0("Year: ", x)} for year. This is
what makes the label text generalize across datasets: it's tied to
whatever \code{facet_row_var} means for your data.
Default \code{as.character} (just prints the raw value).}

\item{row_label_position}{\code{"title"} (bold label above each row's panel,
default) or \code{"ylab"} (as the y-axis label, the old behavior).}

\item{panel_labels}{Logical. Label each row-facet panel with a lowercase
letter. Default \code{TRUE}. Set \code{FALSE} when the plot is itself a panel of a
larger composite figure, where the outer figure supplies the letters and
an inner set would collide with them.}

\item{vline_var}{Numeric column for vertical reference line. Default \code{NULL}.}

\item{vline_value}{Threshold value in \code{vline_var} space. Default \code{NULL}.}

\item{vline_color}{Line color. Default \code{"black"}.}

\item{vline_linetype}{Line type. Default \code{"dashed"}.}

\item{vline_linewidth}{Line width. Default \code{0.7}.}
}
\value{
A ggplot2 object (no \code{facet_row_var}) or a cowplot grid object.
}
\description{
Produces a stacked bar plot of \strong{observed species frequencies}: one bar
per sample, colored by species. This is a pre-fitting diagnostic that shows
the raw community signal in your data before any model is applied. It uses
the same visual format as \code{\link[=eDNA_dmm_structure]{eDNA_dmm_structure()}} so the two plots can be
compared directly: the top panel shows what species are present, the bottom
shows what community they were assigned to.

When \code{facet_row_var} is supplied, one sub-plot is built per row-level and
they are assembled vertically with cowplot. This mirrors the original
make_bar_figure() approach that gives each depth stratum its own
proportional-width panels, the only architecture that keeps panel widths
proportional to sample count via space = "free_x".
}
\seealso{
\code{\link[=eDNA_dmm_structure]{eDNA_dmm_structure()}}, \code{\link[=simulate_eDNA_survey]{simulate_eDNA_survey()}}
}
